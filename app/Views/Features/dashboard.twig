{% extends 'Layouts/main.twig' %} {% block content %}
<div class="container-fluid">
   <div class="row">
      <div class="col-lg-12">
         <div class="row">
            {% for modulo in sidebar_modulos %}
               <div class="col-md-6 col-lg-3">
                  <div class="iq-card iq-card-block iq-card-stretch iq-card-height">
                     <div class="iq-card-body iq-bg-warning rounded">
                        <div class="d-flex align-items-center justify-content-between">
                           <div class="rounded-circle iq-card-icon bg-warning"><i class="{{ modulo.mini }}"></i></div>
                           <div class="text-left">                                 
                              <h5 class="">{{ modulo.menu }}</h5>
                           </div>
                        </div>
                     </div>
                  </div>
               </div>
            {% endfor %}
         </div>
      </div>
   </div>
  <div class="row">

  </div>
</div>
{% endblock %}

{% block javascriptAPP %}

{% endblock %}


